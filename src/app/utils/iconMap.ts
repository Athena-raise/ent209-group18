import { Droplet, BedDouble, Activity, Apple, Heart, Dumbbell, type LucideIcon } from "lucide-react";
import type { RhythmIconName } from "../../store";

export const iconMap: Record<RhythmIconName, LucideIcon> = {
  Droplet,
  BedDouble,
  Activity,
  Apple,
  Heart,
  Dumbbell,
};
